server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-username/microservices-config-repo
          # HOẶC dùng local filesystem:
          # uri: file:///${user.home}/config-repo